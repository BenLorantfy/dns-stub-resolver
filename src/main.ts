function resolveName(name: string) {
    // TODO: create UDP socket, create DNS payload, and return answer
    return '123.45.23.65';
}

// TODO: get name form stdin
console.log(resolveName('google.com'));
